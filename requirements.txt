flask>=2.0.0
psutil>=5.8.0
requests>=2.25.0
opencv-python>=4.8.0
mediapipe>=0.10.9
numpy>=1.24.0
google-generativeai>=0.3.0
python-dotenv>=1.0.0

# Security & Monitoring
flask-httpauth>=4.8.0
flask-limiter>=3.5.0

# Speech-to-Text
# Pinned to 1.x: the 2.x SDK removed `mistralai.models` / top-level `Mistral`,
# which breaks src/stt_manager.py (Voxtral realtime transcription).
mistralai[realtime]>=1.9,<2
pyaudio>=0.2.14
# mediapipe requires protobuf <5; the mistralai[realtime] extra otherwise pulls 5.x.
protobuf>=4.25.3,<5

# Wake Word Detection ("Hey Jarvis")
openwakeword>=0.6.0
